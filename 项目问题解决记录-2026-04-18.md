# nftables-web-v2 项目问题解决记录

## 概述
记录 2026年4月17-18日发现并解决的关键问题，包括问题分析、解决方案和经验总结。

## 问题列表

### 1. IPv6 处理 BUG 系列（2026-04-17）
**状态**: 🔄 已分析，待代码修复

#### 问题描述
- DDNS 报错: "No or private or invalid IP 'fdaa:9c8a:01d5:0000:0011:32ff:fe23:16c0' given!"
- IPv6 端口转发失效
- 地址拼接产生无效 IPv6 地址

#### 根本原因分析
1. **BUG1**: `_get_wan_ipv6_prefix()` 无法处理缩写 IPv6 地址
2. **BUG2**: 地址拼接产生双冒号无效地址
3. **BUG3**: 接口检测硬编码 `pppoe-wan`
4. **网络环境**: WAN 接口无公网 IPv6，回退到 LAN 的 ULA 地址

#### 修复方案
- 新增 `expand_ipv6()` 函数处理缩写地址
- 新增 `get_ipv6_interfaces()` 自动检测接口
- 修改 `_get_wan_ipv6_prefix()` 优先选择公网地址
- 修复 `_extract_ipv6_suffix()` 使用展开后的地址

#### 代码审查
- **审查人**: ceshi Agent
- **审查结果**: 方案可行，建议使用 Python 内置 `ipaddress` 模块
- **优先级**: P0（双冒号拼接）> P1（缩写处理）> P2（接口硬编码）

#### 相关文件
- 经验总结: `03-知识库/编程/Python-IPv6地址处理BUG修复经验.md`
- 修复代码: `/tmp/ipv6_fix.patch`
- 测试脚本: `/tmp/test_ipv6_fix.py`

---

### 2. 反代大文件上传问题（2026-04-18）
**状态**: ✅ 已解决并部署

#### 问题描述
- 300MB+ 文件无法通过 nginx 反代上传到 DSM
- 小文件（几 MB）正常
- 直接访问 DSM (`192.168.100.226:41820`) 可以上传大文件

#### 根本原因
nginx 全局配置限制:
```nginx
client_max_body_size 128M;  # 最大 128MB
```

#### 解决方案
修改 DSM 反代配置 (`/etc/nginx/conf.d/fzyto.top-41825.conf`):
```nginx
location / {
    client_max_body_size 500M;  # 增加到 500MB
    # 保持现有优化配置
    proxy_buffering off;
    proxy_request_buffering off;
}
```

#### 实施步骤
1. 备份原始配置
2. 添加 `client_max_body_size 500M`
3. 测试配置: `nginx -t`
4. 重载配置: `nginx -s reload`

#### 存储空间考量
- **OpenWrt 存储有限**: `/overlay` 87.4MB (已用 76%)
- **临时目录**: `/var/lib/nginx/` 在 `/tmp` (tmpfs, 499.6MB)
- **关键优化**: `proxy_request_buffering off` 实现流式传输，不缓存整个文件

#### 监控命令
```bash
# 监控存储使用
watch -n 1 'free -h; df -h /tmp; du -sh /var/lib/nginx/body'
```

#### 相关文件
- 经验总结: `03-知识库/运维/nginx-反代大文件上传问题解决经验.md`
- 配置文件: `/etc/nginx/conf.d/fzyto.top-41825.conf`
- 备份文件: `/etc/nginx/conf.d/fzyto.top-41825.conf.backup.20260418_110059`

---

### 3. 网络环境分析（2026-04-17）
**状态**: ✅ 已完成分析

#### 发现的问题
1. **两个 odhcp6c 进程**:
   - `eth1` 上的进程失败 ("Address not available")
   - `pppoe-wan` 上的进程成功获取 IPv6

2. **IPv6 接口配置**:
   - `wan6`: 手动配置，绑定到 `eth1` (错误)
   - `wan_6`: 系统自动创建，绑定到 `pppoe-wan` (正确)

3. **公网 IPv6 已获取**:
   - 地址: `240e:390:472e:4fe0::1/60`
   - 接口: `br-lan`
   - 连通性: 正常

#### 配置问题
```bash
# 当前错误配置
network.wan6.device='eth1'      # 应该绑定到 pppoe-wan
network.wan6.proto='dhcpv6'

# 自动创建的正确配置
network.wan.ipv6='auto'         # 导致系统创建 wan_6
```

#### 建议修复
```bash
# 方案1: 删除错误的 wan6 配置
uci delete network.wan6
uci commit network

# 方案2: 修复 wan6 配置
uci set network.wan6.device='pppoe-wan'
uci set network.wan6.proto='dhcpv6-pd'
uci commit network
```

---

## 经验总结

### 1. 问题诊断方法论
1. **现象收集**: 错误信息、日志、复现步骤
2. **环境分析**: 网络拓扑、配置状态、资源限制
3. **根本原因**: 代码 BUG、配置错误、环境限制
4. **解决方案**: 临时修复、永久修复、预防措施

### 2. 技术要点
- **IPv6 地址处理**: 缩写展开、段补零、地址验证
- **nginx 优化**: 流式传输、缓冲区管理、大小限制
- **OpenWrt 限制**: 存储空间、内存使用、临时目录
- **网络诊断**: 接口检测、路由分析、服务状态

### 3. 文档重要性
- **问题记录**: 便于后续参考和知识积累
- **解决方案**: 标准化处理流程
- **经验总结**: 避免重复踩坑
- **团队协作**: ceshi 代码审查的价值

### 4. 工具使用
- **OpenClaw**: 多 Agent 协作、代码审查、问题诊断
- **SSH 诊断**: 系统状态检查、配置验证、实时监控
- **文档系统**: Obsidian vault 知识管理

---

## 待办事项

### 高优先级
- [ ] 应用 IPv6 BUG 修复代码到主路由器
- [ ] 测试 IPv6 端口转发功能
- [ ] 监控 DDNS 更新状态

### 中优先级
- [ ] 修复 `wan6` 网络配置
- [ ] 更新 nftables-web 反代模板
- [ ] 添加大文件上传配置选项

### 低优先级
- [ ] 完善监控脚本
- [ ] 更新项目文档
- [ ] Git 提交修复代码

---

## 参与人员
- **问题发现与诊断**: OpenClaw 总管
- **代码审查**: ceshi Agent
- **解决方案实施**: OpenClaw 总管
- **文档整理**: OpenClaw 总管

## 时间线
- **2026-04-17 21:06**: 开始分析 IPv6 DDNS 错误
- **2026-04-17 21:27**: 发现公网 IPv6 已正常获取
- **2026-04-17 21:40**: 完成 IPv6 BUG 分析，启动代码审查
- **2026-04-17 21:49**: ceshi 完成代码审查
- **2026-04-18 07:44**: 开始分析反代大文件上传问题
- **2026-04-18 10:54**: 发现 nginx 大小限制问题
- **2026-04-18 11:01**: 完成反代配置修复
- **2026-04-18 11:05**: 完成经验文档整理

---

**最后更新**: 2026-04-18 11:06  
**文档状态**: ✅ 已完成  
**下一步**: 应用 IPv6 代码修复，测试完整功能